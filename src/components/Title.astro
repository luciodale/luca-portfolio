---
interface Props {
  color?: string;
}

const { color } = Astro.props;
---

<h2
  class:list={[
    "text-3xl md:text-4xl font-semibold mb-4 uppercase",
    color ?? "text-gray-900",
  ]}
>
  <slot />
</h2>
